{
  "name": "voter-control-app",
  "version": "1.0.0",
  "description": "An application to manage and track voters for a union election.",
  "main": "index.js",
  "scripts": {
    "start": "live-server --port=8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
